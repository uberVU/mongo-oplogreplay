OplogWatcher
   * implement regexp matching in OplogWatcher (db + collection-level)

OplogReplayer
   * ability to start the oplog from a certain point in time (for misc->meta split)
   * use logging instead of print

other
   * ubervu OplogReplay:
      * update last_ts only every 10K ops
      * don't do deletes
      * updates HACK

   * OplogPrinter

   * make package